# تست

```bash
python manage.py test instagram.tests.automation
```

پوشش: normalize فارسی، matching، encrypt، error map، allowlist، oauth state، webhook verify/signature، idempotency، rule/flow، human takeover، tenant isolation.

Meta با mock/adapter محلی — بدون حساب واقعی.
